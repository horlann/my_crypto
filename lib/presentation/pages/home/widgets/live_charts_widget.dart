import 'package:flutter/material.dart';

class LifeChartWidget extends StatelessWidget {
  const LifeChartWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
